import { useState } from "react";
import { DBUser } from "../../utils/interfaces/userInterfaces";

export const UserProfile = () => {

    const [user, setUser] = useState<DBUser>();

    return (
        <div>
            
        </div>
    );

}